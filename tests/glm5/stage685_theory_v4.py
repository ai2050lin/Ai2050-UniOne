#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
Stage685: P39 统一理论v4.0——整合所有实验发现的最终版本

本文件是理论文档，不运行实验，而是生成v4.0的理论总结。
"""

from datetime import datetime
from pathlib import Path

OUTPUT_DIR = Path(__file__).resolve().parents[2] / "tests" / "glm5_temp"
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

THEORY_V4 = """
================================================================================
语言编码统一理论 v4.0 —— 最终整合版
================================================================================
时间: {timestamp}

基于P28-P39共12个实验阶段的系统性验证，在4个主流LLM（Qwen3-4B, DeepSeek-R1-7B, 
GLM4-9B, Gemma4-9B）和多个tiny GPT-2上完成。

================================================================================
一、五个公理（修正版）
================================================================================

【第一公理：高维几何】
  表述: 在充分训练的高维模型中，不同语义内容的hidden state方向
        在原始空间中高度对齐（cos>0.87），但通过微小方向差异编码语义。
  数学: ||h_a - h_b|| ≈ √(||h_a||² + ||h_b||² - 2||h_a||||h_b||cos(θ))
        其中cos(θ) = 0.87~0.95（远未达到正交）
  修正(P32): "正交性"在大模型最终层不成立——方向高度对齐是RMSNorm的
           必然结果（norm标准化使所有向量被"拉向"同一方向）
  关键: 高维正交性只在随机方向上成立，而训练优化使所有hidden state
        趋向共享的"语义基底方向"

【第二公理：信息域约束】
  表述: 不同语言能力在hidden state空间中的组织方式由模型架构和
        训练策略共同决定，可分为SEPARATED和DENSE两种策略。
  数学: 纠缠度 ≈ C · √(K_domain/d) · f(RL, data, arch)
  修正(P33): f(RL)不是简单乘性因子，而是根本性地改变了编码策略
  修正(P35): 信息域不是空间分离的聚类，而是方向对齐上的微小偏移
  关键发现:
    - Qwen3: Silhouette=0.064（弱SEPARATED）→ 独立维度策略
    - DS7B: Silhouette=-0.065（极端DENSE）→ 共享维度策略
    - DS7B的PCA90仅13维（其他模型45-52维）→ 信息超级压缩

【第三公理：归一化隔离】
  表述: LayerNorm/RMSNorm通过压缩信号来实现不同信息通道之间的隔离。
  数学: 残存 ≈ (1-ρ_avg)^N_norms（Markov链衰减模型）
  新发现(P36): RMSNorm使norm趋向恒定（域间差异仅2.5%），但
             DS7B例外（域间差异19.5%——未完全约束）

【第四公理：信号聚焦】
  表述: 语义信息在Transformer层的传播中经历"编码→处理→聚焦"三阶段。
  数学: 聚焦比 = cos_avg(后期)/cos_avg(前期) > 1.5
  新发现(P38): 信号强度(||h||)在后期层显著增大（INV-332确认）
             但域分离度不一定在后期最高——存在"倒拱形"模式

【第五公理：Logit精确】
  表述: 最终输出logit与hidden state的线性投影精确一致。
  数学: margin = ||h_L|| · ||Δw|| · cos(h_L, Δw)（恒等式）
  补充(P34): 多步累积方程 avg_logprob ≈ log(avg_margin) - C，C≈1.5

================================================================================
二、层动力学模型（P38核心发现）
================================================================================

不同模型展现出截然不同的信息流动模式：

【Qwen3模式：倒拱形】
  L0-2: sil从0→-0.08（初始编码，域分离最差）
  L7-9: sil达到峰值0.14（域分离最强！）
  L10-22: sil稳定在0.11-0.12（持续处理）
  L28-36: sil从0.11下降到0.08（聚焦导致域融合）
  → 语义区分在中间偏早期最强，后期聚焦牺牲了域分离度

【DS7B模式：U形】
  L2-4: sil=+0.04（短暂的域分离窗口）
  L5-26: sil≈-0.21（持续DENSE，所有信息深度融合）
  L27-28: sil回升到-0.08（后期分离恢复）
  → RL训练创造了"深度融合区"（L5-L26），推理可能发生在融合区

【GLM4模式：平稳型】
  全程sil≈0.08-0.12，变化很小
  → 信息处理均匀分布在各层

【Gemma4模式：U形（轻量版）】
  与DS7B类似但幅度更小
  → 多模态训练引入了部分DENSE特征

================================================================================
三、方向+Norm协同编码（P36核心发现）
================================================================================

【核心结论：方向承载几乎所有区分信息】

  | 模型 | 方向Sil | Norm Sil | Norm域间差异% |
  |------|---------|----------|-------------|
  | Qwen3 | **0.090** | -0.273 | 2.5% |
  | DS7B | -0.081 | -0.205 | **19.5%** |
  | GLM4 | **0.067** | -0.245 | 2.7% |
  | Gemma4 | -0.006 | -0.201 | -- |

  → 方向是语义区分的主要载体（Silhouette更高）
  → Norm被RMSNorm强烈约束（域间差异仅2-3%）
  → DS7B是例外：Norm域间差异达19.5%（RMSNorm约束较弱）

================================================================================
四、信息瓶颈理论（P37核心发现）
================================================================================

【DS7B的信息超级压缩】

  DS7B的PCA结构：
    PC1: 43.3%（编码spatial↔style的区分）
    PC2: 17.6%（编码style↔temporal的区分）
    → 前两个主成分就解释了60.9%的方差
    → PCA90=13维（vs Qwen3的45维）

  Qwen3的PCA结构：
    PC1: 20.9%（编码relation↔syntax的区分）
    PC2: 10.6%（编码relation↔style的区分）
    → 方差分布更均匀
    → PCA90=45维

  解读: DS7B将所有语义信息压缩到极少数"超级维度"中，
       这可能是RL训练的"效率优化"——用更少的维度做更多的事，
       但代价是维度间的纠缠度极高（std=0.206）。

================================================================================
五、多步累积方程（P34核心发现）
================================================================================

【线性→对数转换公式】

  avg_logprob ≈ log(avg_margin) - C

  其中C ≈ 1.5（跨模型稳定的常数偏移）

  四模型验证:
    Qwen3:   gap = 1.39~2.03
    DS7B:    gap = 0.97~1.75
    GLM4:    gap = 1.26~2.05
    Gemma4:  gap = 1.17~2.08

  → "100倍低估"问题的根源：margin是线性尺度，log_prob是对数尺度
    log(e^x) = x，所以log(margin)和avg_logprob之间差的是softmax的
    归一化常数，约1.5个自然对数单位

================================================================================
六、不变量总结
================================================================================

已确认（5个）:
  INV-305: 归一化是隔离核心机制
  INV-306: 归一化自适应压缩
  INV-308: 编码策略由信息域数量决定
  INV-313: 信号聚焦（后期层放大）
  INV-332: 后期层信号强度更高

部分确认（3个）:
  INV-316: 五公理从训练涌现（A4/A5成立，A1需要高维）
  INV-322: log(margin) - avg_lp ≈ 常数（C≈1.5，但范围较大）
  INV-329: Top1主成分编码语言通用特征

需修正（5个）:
  INV-317: 纠缠度≈C√(K/d)（需要f修正因子，DS7B偏差16x）
  INV-320: 推理难度↑→纠缠度↑（仅GLM4确认）
  INV-321: 信息密度↑→纠缠度↑（仅Gemma4确认）
  INV-326: Norm区分力 > 方向区分力（实际反转！方向>Norm）
  INV-331: 中间层域分离最低（Qwen3/GLM4反转，仅DS7B/Gemma4确认）

================================================================================
七、剩余瓶颈与突破方向
================================================================================

【理论层面】
  1. 方向对齐(cos>0.87)的机制——为什么所有文本方向如此相似？
     → 可能是RMSNorm + 残差连接的必然结果
     → 需要数学证明：RMSNorm是否迫使hidden state趋向共享方向
  2. 修正因子f(RL,data,arch)的数学形式
     → DS7B的纠缠度0.282偏离理论预测(0.017)16x
  3. 多步累积方程中C的精确值——为什么是1.5？
     → 可能与softmax温度和词汇量有关

【实验层面】
  4. 高维阈值d*的精确确定（需要d>512）
     → 当前GPU限制无法训练大维度模型
  5. 跨层注意力权重分析——注意力如何引导信息流动？
  6. 消融实验——移除特定层后能力如何变化？

【应用层面】
  7. 基于"层动力学"的模型诊断工具
     → 通过分析sil曲线判断模型类型（SEPARATED/DENSE/U形）
  8. 基于"信息瓶颈"的模型压缩指导
     → DS7B的PCA90=13维暗示可以大幅压缩而不损失性能

================================================================================
八、对AGI目标的意义
================================================================================

我们已经从"五公理"框架发展到包含层动力学、方向-norm协同编码、
信息瓶颈理论的完整体系。最关键的洞察：

  **语言编码不是在高维空间中"分散存储"信息，而是在一个高度对齐的
   "语义基底方向"上，通过微小方向偏移和norm调制来编码不同的语义内容。**

这意味着：
  1. 语言模型的"理解"可能就编码在这些微小的方向偏移中
  2. 不同模型的"智能差异"可能主要来自于：
     - 对齐方向的选择（哪些方向成为"基底"）
     - 偏移的精度（多大的方向差异编码多大的语义差异）
     - 信息分布的集中度（PCA90=13 vs 45维）

下一步的终极目标：
  → 证明"语义基底方向"的存在性和唯一性
  → 建立从文本到方向偏移的可预测映射
  → 理解RL训练如何改变"基底选择"策略
"""

timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
content = THEORY_V4.format(timestamp=timestamp)

# 输出到控制台
print(content)

# 保存
save_path = OUTPUT_DIR / f"stage685_theory_v4_{datetime.now().strftime('%Y%m%d_%H%M')}.md"
with open(save_path, "w", encoding="utf-8") as f:
    f.write(content)
print(f"\n理论文档已保存: {save_path}")
